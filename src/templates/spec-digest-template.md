# <Full Document Title> — Digest

**Scope:** <One-line hook used by `guide_search` ranking; concise statement of what this document defines.>
**Component:** <RM | AM | BASE | SM | QUERY | LANG | PROC | CDS | CNF | ITS-REST | ITS-XML | TERM>
**Document:** <upstream doc ID, e.g. ehr, data_types, ADL2, openehr_platform>
**Release:** <branch or release tag; default `development` for living specs; use `Release-X.Y.Z` for a pinned point-in-time digest>
**Spec URL:** <canonical HTML URL>
**Markdown URL:** <canonical .md URL, or `N/A` if no .md twin exists (e.g. OpenAPI-rendered docs)>
**Last updated:** <YYYY-MM-DD>
**Related:** <optional, comma-separated openehr://guides/specs/... URIs for sibling digests>
**Keywords:** <comma-separated short search terms>

---

## Purpose

<2–4 sentences. What this spec document defines and why it exists.>

## Scope

<Bulleted list. What is normative in this doc and what it deliberately does not cover.>

- In: …
- Out: …

## Key Classes / Constructs

<Bulleted list of the top 5–12 named entities (classes, types, constructs) a reader must know. Every name must appear in the upstream spec. Each entry: `CLASS_NAME` — one-line role.>

- `CLASS_NAME` — …

## Relations to Other Specs

<Bulleted list. Which other spec documents this one depends on / is depended on by. Use component-doc IDs (e.g. `RM/common`, `AM/ADL2`), not URLs.>

- Depends on: `RM/common`, `RM/data_types`
- Consumed by: `AM/…`, `QUERY/…`

## Architectural Placement

<1–2 sentences locating this doc within the component and the overall openEHR stack.>

## When to Read the Full Spec

<1–2 sentences. Concrete signals that the digest is insufficient and the full `.md`/HTML is needed.>

## References

- Full spec (HTML): <Spec URL>
- Full spec (Markdown): <Markdown URL>
- Related digests: specs/<sibling>, specs/<sibling>
